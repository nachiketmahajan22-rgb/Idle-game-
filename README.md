# Blade Hunter: Idle Slayer

A dark-fantasy incremental/idle game — tap to strike, recruit blade hunters and rift turrets to grind Essence automatically, unlock achievements, and Reforge the Blade to prestige for permanent power. Visual theme is a Survivor.io-inspired blade-hunter-vs-monsters reskin on top of a classic clicker/incremental core loop (not a bullet-heaven combat sim).

Packaged as an Android app for the Play Store via [Capacitor](https://capacitorjs.com/), wrapping a plain HTML/CSS/JS game with no build step of its own.

## Project layout

```
game/            The entire game. Open game/index.html directly in any browser to play/develop.
  index.html
  css/style.css
  js/            Load order matters — see the <script> tags in index.html.
android/         Generated by `npx cap add android` — a standard Capacitor Android/Gradle project.
store-assets/    Master icon art + export-size checklist for Play Store submission.
docs/            Play Store publishing checklist + privacy policy placeholder.
```

## Local development

No build tooling is required for the game itself. Just open it in a browser:

```
open game/index.html          # macOS
xdg-open game/index.html      # Linux
```

Or serve it locally (recommended for realistic testing, since some browsers restrict `localStorage` under `file://`):

```
npx http-server game -p 8080
# then visit http://localhost:8080
```

## Building the Android app

1. Install dependencies:
   ```
   npm install
   ```
2. Generate the native Android project (first time only):
   ```
   npx cap add android
   ```
3. Sync web assets into the native project (run after every change to `game/`):
   ```
   npx cap sync
   ```
4. Open in Android Studio to build/run/sign:
   ```
   npx cap open android
   ```
   Or build a debug APK from the CLI:
   ```
   cd android && ./gradlew assembleDebug
   ```

**Before you ship a real build**, you must supply your own:
- `appId` in `capacitor.config.json` (a reverse-DNS package name you own in Play Console — this is permanent once published, so pick it deliberately, e.g. replace `com.bladehunter.idle`).
- A real AdMob App ID and Rewarded Ad Unit ID (replace the `ca-app-pub-0000000000000000~0000000000` placeholders in `capacitor.config.json`, `android/app/src/main/AndroidManifest.xml`, and `game/js/ads.js`), and set `AdMob.initializeForTesting` to `false` in `capacitor.config.json`.
- A signing/upload keystore (generate locally or in CI secrets — **never commit it**; `.gitignore` already excludes `*.keystore`/`*.jks`).
- A publicly hosted privacy policy URL for the Play Console listing — `docs/PRIVACY_POLICY.md` is a starting draft; GitHub Pages is a free way to host it from this same repo.

See `docs/PUBLISHING.md` for the full Play Console submission checklist.

## Ads

Rewarded video ads are **opt-in only** — no interstitials, no banners, no forced ads during play. The player taps a "Watch Ad" button to receive a temporary boost (see `game/js/ads.js` and the Boosts panel in-game). In a plain desktop browser (no Capacitor/AdMob available), these buttons render disabled so the game is fully playable ad-free during development.

## Save data

Progress autosaves to `localStorage` under the key `bladeHunterIdle_save_v1`. No account, no network calls, no data leaves the device.
