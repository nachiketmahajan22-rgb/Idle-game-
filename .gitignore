node_modules/

# Android build output / local machine config (generated, never commit)
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/captures/
android/*.iml
android/.idea/

# Signing keys — never commit
*.keystore
*.jks

# OS / editor cruft
.DS_Store
